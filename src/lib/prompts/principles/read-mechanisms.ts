/**
 * READ THE MECHANISMS — the "artifacts + key actors carry the operative
 * rules" discipline.
 *
 * Each artifact's world-graph nodes and each key-actor's continuity define
 * what's POSSIBLE in the world. An unactivated mechanism, an unused method,
 * an unaddressed source — these are strong candidates for variables /
 * scenarios / next-arc levers. The model that ignores the mechanisms loaded
 * into the substrate is generating from genre-fiction defaults instead of
 * THIS world's actual machinery.
 *
 * Used by: Compass cohort generation, Present extraction, direction proposal,
 * world expansion suggestions.
 */

export const PRINCIPLE_READ_MECHANISMS = `READ THE MECHANISMS. Artifacts and key actors carry the operative rules and capabilities loaded into the world. In fiction these are powers and lore; in simulation they are the rule set itself; in argument they are methods, sources, theoretical commitments; in record they are charters, institutions, conventions. Their world-graph nodes define what's POSSIBLE. An unactivated mechanism / unused method / unaddressed source is a strong candidate for the read — the substrate is telling you what could fire next.`;

/**
 * PARADIGM FIDELITY — the "honour the operator-declared paradigm" discipline.
 *
 * Every paradigm-aware prompt receives a work-identity declaration. The
 * model must read and refine that identity, never contradict it. A
 * cultivation cohort that drafts in generic-fiction shape, an essay
 * Compass that emits dramatic-arc scenarios, a simulation that authorially
 * rescues the favoured actor — each is paradigm drift. Refine: yes (the
 * compass cues should sharpen the operator's framing with the substrate's
 * concrete details). Contradict: no.
 *
 * Used by: every paradigm-aware surface (Compass, Present, Rescore,
 * Direction, Briefing, Expansion, Surveys, Interviews).
 */

export const PRINCIPLE_PARADIGM_FIDELITY = `PARADIGM FIDELITY. The work-identity block at the top of this prompt declares the operative paradigm + genre + subgenre + title. Honour it. The cohort's vocabulary, attractors, tail shape, and forward-motion shape are paradigm-specific. The substrate (outline, mode, roster, threads, prose profile) can REFINE the declaration with concrete details — never CONTRADICT it. A read that drifts from the declared paradigm to a default genre is paradigm error, not amplitude error; score it sharply low and say so.`;

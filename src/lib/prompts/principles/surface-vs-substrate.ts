/**
 * SURFACE vs SUBSTRATE — the "name forces, not symptoms" discipline.
 *
 * Every reasoning surface (Compass, Present, CRG, PRG) is at risk of naming
 * SYMPTOMS instead of the SUBSTRATE forces that cascade to produce them. The
 * principle says: reach one layer below the visible. Symptoms are what
 * becomes observable; forces are what cascade to produce the symptoms.
 *
 * Used by: paradigm-compass.ts (Compass, Present, Rescore), paradigm-analyst.ts
 * (direction, briefing, expansion), reasoning-graph (CRG nodes), mode/generate
 * (PRG nodes).
 */

export const PRINCIPLE_SURFACE_VS_SUBSTRATE = `SURFACE vs SUBSTRATE. Name FORCES, not symptoms. Symptoms are visible (prices fall, a study gets cited, a character argues); forces are what cascade to produce them. Reach one layer below the visible. A read built from symptoms reads as low signal regardless of how confidently it's scored.`;
